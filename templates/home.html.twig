<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="initial-scale=1">
    <title>{% block title %}Elemele{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
        <style>
            *{
                box-sizing: border-box;
            }
            html,body{
                margin: 0;
                background-color: #0f0f0f;
                color: #ffffff;
                font-family: Arial, sans-serif;
            }
            main {
                width: 100vw;
                height: 100%;
                top: 0;
                position: absolute;
            }
            section{
                width: 100%;
            }
            h1 {
                color: #ffffff;
                font-family: Impact, sans-serif;
                text-align: center;
                font-size: 30px;
                margin-top: 35px;
                margin-bottom: 25px;
            }
            h2{
                text-align: center;
                font-size: 20px;
            }
            h3{
                text-align: center;
                font-size: 18px;
            }
            a{
                color: #ffffff;
                text-decoration: none;
                text-align: center;
            }
            .house-wrap{
                display: flex;
                flex-wrap: wrap;
                align-content: start;
                justify-content: flex-start;
            }
            .house-wrap > a{
                display: flex;
                align-items: center;
                justify-content: center;
                width: 50vw;
                height: 50vw;
                border: solid 2px #fff;
                font-size: 18px;
            }
            .house-wrap > a span{
                overflow-wrap: break-word;
                width: 100%;
            }
            @keyframes rainbow {
                0% {color: #ff9999}
                33.3% {color: #99ff99}
                66.6% {color: #9999ff}
                100% {color: #ff9999}
            }
            h3 > span:nth-child(1){
                animation: rainbow 0.8s infinite linear;
            }
            h3 > span:nth-child(2){
                animation: rainbow 0.8s 0.1s infinite linear;
            }
            h3 > span:nth-child(3){
                animation: rainbow 0.8s 0.3s infinite linear;
            }
            h3 > span:nth-child(4){
                animation: rainbow 0.8s 0.4s infinite linear;
            }
            h3 > span:nth-child(5){
                animation: rainbow 0.8s 0.5s infinite linear;
            }
            h3 > span:nth-child(6){
                animation: rainbow 0.8s 0.6s infinite linear;
            }
            .flash-notice{
                position: absolute;
                color: #ff9999;
                font-size: 25px;
                text-align: center;
                width: 100%;
                height: 100%;
                padding-top: 50%;
                background-color: #0a0a0a99;
            }
        </style>
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
        <script>
            function hide(elem){
                elem.style.display = 'none';
            }
            addEventListener("load", function() {
                let viewport = document.querySelector("meta[name=viewport]");
                viewport.setAttribute("content", viewport.content + ", height=" + window.outerHeight);
                let main = document.querySelector('main');
                main.style.height = window.outerHeight + 'px';
            })
        </script>
    {% endblock %}
</head>
<body>
{% block body %}
    <main>
        {% for message in app.flashes('error') %}
            <div class="flash-notice" onclick="hide(this)">
                {{ message }}
            </div>
        {% endfor %}
        <h1>ElemeleApp</h1>
        <h2>Cześć, {{ owner_username }}</h2>
        <h3><span>☆</span> <span>☆</span> <span>☆</span> Twoje mieszkania <span>☆</span> <span>☆</span> <span>☆</span></h3>
        <section class="house-wrap">
            {% for house in houses %}
                <a href="/houses/{{ house.id }}">
                <span>{{ house.name }}</span>
                </a>
            {% endfor %}
            <a href="/create-house">
            <span>+</span>
            </a>
        </section>
    </main>
{% endblock %}
</body>
</html>
